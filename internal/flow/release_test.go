package flow_test

import "testing"

func TestService_ReleaseBranch(t *testing.T) {
	type input struct {
	}
	type output struct {
	}
	tt := []struct {
		name   string
		input  input
		output output
	}{
		{
			name:   "",
			input:  input{},
			output: output{},
		},
	}
	for _, tc := range tt {
		t.Run(tc.name, func(t *testing.T) {

		})
	}
}
